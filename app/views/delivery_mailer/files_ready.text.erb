<%= @send.user.email_address %> sent you <%= pluralize(@send.files.count, "file") %>.

<% if @send.message.present? %>“<%= @send.message %>”

<% end %>See your files: <%= delivery_url(public_id: @send.public_id, anchor: "token=#{@access_token}") %>

This is a private Campdoc delivery meant for <%= @send.recipient_email %>.
